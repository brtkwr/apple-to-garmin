name: 🧪 Run Tests

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main ]

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ['3.11', '3.12', '3.13']

    steps:
    - name: 📥 Checkout code
      uses: actions/checkout@v4

    - name: 🐍 Set up Python ${{ matrix.python-version }}
      uses: actions/setup-python@v4
      with:
        python-version: ${{ matrix.python-version }}

    - name: 📦 Install uv
      uses: astral-sh/setup-uv@v4

    - name: 🧪 Run tests
      run: |
        uv run pytest -v

    - name: 🔍 Run basic smoke tests
      run: |
        python convert_to_tcx.py --help
        uv run convert_to_fit.py --help

  lint:
    runs-on: ubuntu-latest
    
    steps:
    - name: 📥 Checkout code
      uses: actions/checkout@v4
      
    - name: 🐍 Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.11'
        
    - name: 📦 Install uv
      uses: astral-sh/setup-uv@v4

    - name: 🔍 Check Python syntax
      run: |
        python -m py_compile convert_to_tcx.py
        python -m py_compile convert_to_fit.py
        python -m py_compile test_convert_to_tcx.py
        
    - name: 📊 Check code structure
      run: |
        echo "Checking for TODO/FIXME comments..."
        grep -n "TODO\|FIXME" *.py || echo "No TODOs found ✅"
        
        echo "Checking file sizes..."
        wc -l *.py
        
        echo "All code structure checks completed ✅"